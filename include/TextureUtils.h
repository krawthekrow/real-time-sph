#pragma once

#include <GL/glew.h>
#include <glm/glm.hpp>

using namespace glm;

class TextureUtils {
public:
    static const int MAX_SCREEN_WIDTH = 1920;
    static const int MAX_SCREEN_HEIGHT = 1080;
};
